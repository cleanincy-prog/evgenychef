# Event-format perimeter concept — discussion mockup — 2026-09-09

Mobile exploration created in response to the user's direct correction: the drawing must continue around
the perimeter, across the top, across the bottom and through the gap between live text and the documentary
photograph. On 2026-09-10 the user approved only that thin-rule structure for `Частный ужин`, explicitly
without any labels. The illustrated routes, captions and the two other service directions remain unapproved.

The raster and SVG mockup files remain deliberately unwired and are not copied to `public/`. Production
uses the selected structural principle as a small live CSS/HTML layer so rejected labels and routes cannot
return with it.

- `01-private-dinner-perimeter-v6.png`: ingredients and tools around the upper/side route, a sauce gesture
  through the middle and seven courses plus the intimate table below.
- `02-private-event-perimeter-v6.png`: griddle/mise en place above, serving gestures between the apertures,
  then four bite families, trays and a restrained guest circulation loop below.
- `03-masterclass-perimeter-v6.png`: demonstration above, feedback through the middle and two practice
  stations with three outcomes below.
- `mobile-preview.html`: exact live Russian copy and the current documentary photos positioned over the
  three candidate underlays.
- `mobile-preview.png`: discussion contact sheet rendered from that HTML at mobile proportions.
- `01-*–v7.png` through `03-*–v7.png`: second-round, 35–40% quieter underlays with enlarged live-photo
  apertures.
- `mobile-preview-v2.html` and `mobile-preview-v2.png`: revised discussion mockup with larger photographs
  and reduced drawing density; neither is connected to the live site.
- `mobile-preview-v3.png`: preserved third-round visual checkpoint after the user rejected the generated
  vignette language as weak and visibly AI-made.
- `mobile-preview-v4.html` and `mobile-preview-v4.png`: semantic revision of that strict SVG language.
  Every visible route now restates only supported live-copy meaning; all unlabelled nodes, speculative
  logistics and decorative pseudo-technical signs are removed.

The raster drawings were generated with the built-in ImageGen tool. They are concept art, not production
assets and not measured technical drawings.

The v3/v4 direction explicitly retires those raster drawings. Its linework is ordinary HTML/SVG derived
from the exact service copy. V4 maps: favourite dishes or seven-course route → cooking for loved ones;
free format → snacks/dishes → easy eating during conversation; and demonstration → cooking together →
dishes → shared table.
