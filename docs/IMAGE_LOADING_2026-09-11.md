# Уточнение: едва заметное размытие — 2026-09-11

По уточнению «лёгкий блюр, прям % 5» радиус фильтра только hero-collage-grid уменьшен с 1.1px до 0.3px. Проценты здесь обозначают субъективно минимальную силу эффекта, а не буквальное значение CSS. Главный портрет и текст не фильтруются. Реальные WebP-фото, их качество и источники сохраняются.

Аудит до правки зафиксирован в DESIGN_REFERENCE_MAP, DESIGN_SYSTEM и IMPLEMENTATION_PLAN. Проверены исходная DOM-иерархия и CSS: фильтр изолирован в фоне, рядом с hero-identity. Других фильтров или анимаций Hero нет. Радиус фильтра не меняет layout, svh-высоту или кадрирование.

Итог по политике проекта: новых файлов нет. Изменены app/globals.css (единственная строка интерфейса), docs/DESIGN_REFERENCE_MAP.md, docs/DESIGN_SYSTEM.md, docs/IMPLEMENTATION_PLAN.md, этот отчёт и docs/RELEASE_2026-09-11.md. Шаблонных элементов нет, композиции не перерабатывались. Референс — прямое уточнение пользователя и текущий одобренный Hero; решений без референса нет, UI-библиотеки не добавлялись. Финальный anti-template аудит пройден.

Hero визуально проверен на 1440, 1280, 1024, 768, 430, 390 и 375px. Фон едва смягчён, портрет и текст чёткие; overflow нет, геометрия всех 63 ячеек при прокрутке вниз/обратно неизменна. Неподвижные размеры и прежние состояния навигации/кнопок сохранены; новых loading/empty/error состояний и форм нет. Сборка, пять HTTP-тестов и diff --check PASS. Проверка выполнена в desktop-браузере с разными viewport; физический iPhone не использовался. Новых рисков или блокеров нет. Предыдущий вариант 05f6ae6 / Sites43 сохранён для отката.

---

# Лёгкие WebP и мягкий фон Hero — 2026-09-11

## Аудит и план до кода

Прямой запрос: снизить качество/вес изображений, использовать WebP или другой быстрый формат, слегка размыть только фоновую часть Hero, не размывать главный портрет и текст. Проверены app/page.tsx, app/globals.css, app/chef-story-video.tsx, app/layout.tsx, worker/index.ts, существующие тесты, активные документы и реальные изображения. Предыдущая версия c4772fb89aacbf7baf1bdb981ce1a7b788a24bbc / Sites42 сохраняется для отката.

68 уникальных растров страницы с постером видео занимают 14 167 565 байт, из них 63 источника коллажа — 8 389 641 байт. Особенно тяжёлые: outdoor-crepes PNG 4 372 392 байт, toast PNG 2 086 296, conversation PNG 1 939 296, plate WebP 1 525 162. Одинаковые большие исходники используются в маленьких ячейках и крупных блоках. Главный портрет 576×1280, 57 075 байт. Все 63 фото сейчас eager, поэтому тяжёлый PNG запрашивается уже первым экраном.

План: подготовить WebP-производные с сохранением оригиналов. Для каждой из 63 ячеек — ширины до 192/384px, качество 54; сохранить исходную пропорцию и прежнюю cover-композицию. Для главного портрета —320/576px и качество 78 без размытия. Для фото форматов —384/768px, качество 70; для награды —320/640px, качество 74; для тарелки —480/960px, качество 76 с исходной альфой; для двух карандашных иллюстраций —480/960px и WebP lossless, чтобы сохранить бумажные цвета и штрихи после масштабирования. Постер видео —540px, качество 70. Уменьшение оригинальных размеров только до указанных максимумов, без увеличения маленьких исходников. Подключить srcset/sizes по существующим областям отображения, оставить lazy у нижних блоков и высокий приоритет только главному портрету. Обновить пути в существующих проверках, не менять видео.

Размытие: постоянный CSS blur(1.1px) только на hero-collage-grid внутри существующего обрезающего контейнера. hero-identity с портретом и заголовком — соседний элемент, вне фильтра. Без анимации, transform/масштабирования и изменения стабильной svh-высоты. Новых компонентов и UI-библиотек не требуется. Все текущие блоки, фото, подписи, шрифты, цвета, границы, позиции и мобильные сетки остаются. Шаблонных элементов не обнаружено.

Референс визуальной правки — прямой запрос на лёгкое размытие и существующая одобренная композиция. Технические параметры сжатия выбраны по реальным размерам ячеек и проверяются по изображению; самостоятельного нового стиля нет. Исходники и соответствие производных записать в scripts/site-image-manifest.json и отчёт экономии в docs/IMAGE_OPTIMIZATION_STATS.json. Кодирование через установленный cwebp, без генеративной обработки; формат/качество/resize описаны в [официальной документации cwebp](https://developers.google.com/speed/webp/docs/cwebp).

Навык web-perf не может выполнить trace: Chrome DevTools MCP отсутствует. Его инструкция «If unavailable, STOP—the chrome-devtools MCP server isn't configured» относится к этой диагностике; полный trace не проводится. Для запрошенной оптимизации достаточно измеримых размеров файлов, проверки выбранных srcset источников, загрузки и визуальной проверки. Не заявлять непроверенные LCP/INP или ускорение в секундах.

Проверить семь обязательных ширин, полный кадр награды, бумажные края иллюстраций, сохранённую альфу тарелки, читаемость портрета/текста, фильтр только на фоне, отсутствие overflow и стабильность ячеек при скролле. Затем сборка и существующие HTTP-тесты, выпуск в тот же GitHub main и публичный evgenychef.com.

## Результат и вес

Созданы 143 WebP-производные. Набор из одного максимального варианта каждого изображения страницы с постером: 1 950 318 байт вместо 14 167 565 (−86,23%). Набор минимальных вариантов —654 552 байта. Коллаж —311 156…776 776 байт вместо 8 389 641 (−90,74% даже с максимальными вариантами). Это размеры файлов, а не замер скорости сети; браузер выбирает варианты по ширине, плотности экрана и кэшу. Видео в эти суммы не входит и не менялось. [Полные размеры и соответствие исходникам](IMAGE_OPTIMIZATION_STATS.json).

`sizes` уточнены по фактической CSS-геометрии: desktop-иллюстрации ограничены 450/440px, тарелка —примерно 428px. Это не даёт браузеру DPR1 выбирать 960w для маленьких блоков. В браузере после обновления страницы подтверждены 480w для обеих иллюстраций и тарелки на 1440/1280/1024; на 768 тарелка получает 960w по её ширине 600px. При уменьшении окна браузер может оставить уже закэшированный более крупный вариант.

Все 143 файла декодированы и проверены по размеру. SHA-256 всех исходников совпадают с манифестом. Прозрачность тарелки сохранена в обоих вариантах; у исходного crepes alpha полностью 255, поэтому удаление непрозрачного alpha-канала кодером корректно. Иллюстрации —lossless после уменьшения, их бумажный фон и штрихи сохранены.

## Проверка

| Размер viewport | Изображения | Горизонтальное переполнение | Скролл вниз / обратно | Чёткость центра Hero |
|---|---|---|---|---|
|1440×900|PASS|нет|геометрия ячеек неизменна|без filter|
|1280×900|PASS|нет|геометрия ячеек неизменна|без filter|
|1024×900|PASS|нет|геометрия ячеек неизменна|без filter|
|768×1024|PASS|нет|геометрия ячеек неизменна|без filter|
|430×932|PASS|нет|геометрия ячеек неизменна|без filter|
|390×844|PASS|нет|геометрия ячеек неизменна|без filter|
|375×812|PASS|нет|геометрия ячеек неизменна|без filter|

Визуально проверены Hero, полный кадр награды, три формата, рисунки, тарелка, постер и нижние блоки. Все 71 img загружены после прокрутки. Только collage-grid имеет blur(1.1px); у портрета, h1 и всех их предков нет CSS-filter. Исходное cover-кадрирование фото и contain тарелки/форматов сохранены. При первой смене брейкпоинта проверка успела увидеть загружаемый новый вариант; повторная проверка подтвердила загрузку без ошибки и сдвига. Размеры зарезервированы width/height и существующей сеткой.

Проверены читаемость и переносы, отсутствие наложений, края рисунков и альфа тарелки. Навигация и кнопки сохраняют прежние размеры и состояния. Клавиатурный Tab показывает skip-link с видимым 2px outline. Форм на странице нет; новые интерактивные, loading/empty/error состояния не добавлены. Плеер остаётся paused, без autoplay; постер загружается, оригинальное видео отдаёт 206 и байтовый диапазон. Повторная проверка сценария ошибки плеера не требуется: код обработчика не менялся.

Production build, ESLint, git diff --check и пять HTTP-тестов —PASS. HTTP-проверка охватывает все responsive URL, WebP MIME, intrinsic dimensions, alt, srcset/sizes, локальный noindex, public canonical/robots/sitemap, ссылки и оригинальное видео. Параллельные HEAD-запросы ограничены восемью, чтобы не исчерпывать локальные сокеты Node/macOS.

## Итоговый отчёт по политике проекта

1. Созданы: `public/media/web/` (143 файла), `app/site-images.ts`, `scripts/optimize-site-images.mjs`, `scripts/site-image-manifest.json`, этот отчёт и `docs/IMAGE_OPTIMIZATION_STATS.json`.
2. Изменены: `app/page.tsx`, `app/chef-story-video.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, пути в `scripts/visual-qa.mjs` и `scripts/qa-plate-captions.mjs`, `docs/DESIGN_REFERENCE_MAP.md`, `docs/DESIGN_SYSTEM.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/RELEASE_2026-09-11.md`.
3. Шаблонных элементов аудит не обнаружил.
4. Композиционные переработки не потребовались; изменены доставка растров и запрошенное размытие фона.
5. Референсы: прямой запрос пользователя, реальные исходники, одобренная «План вечера»/Caveat композиция. Новое фото «МастерШеф» сохранено.
6. Визуальных решений без референса нет. Технические параметры encoder выбраны по фактическим размерам и проверке.
7. UI-библиотеки не добавлялись; готовые шаблоны не использовались.
8. Desktop/tablet/mobile проверены на семи обязательных ширинах; все проверки выше пройдены. Финальный anti-template аудит: индивидуальность страницы и мобильная композиция сохранены, новые карточки, декор, повторяющиеся секции и заглушки отсутствуют.
9. Ограничения: desktop-браузер с изменением viewport, без физического iPhone; Core Web Vitals и сетевые секунды не измерялись. Максимальные размеры ограничивают детализацию при сильном увеличении; оригиналы сохранены для последующей перекодировки. Блокеров реализации нет.

Выпуск в прежние GitHub main и Sites-проект `appgprj_6a981b9ab8708191b17ebf66cc5f818f`, аудитория public и домен evgenychef.com сохраняются. SHA и версия фиксируются в Git/Sites. Предыдущая версия c4772fb / Sites42 доступна для отката.
