# Опустить этап03 — 2026-09-13

## Аудит и план до правки

Источник — прямое «03 тоже нужно сделать ниже» после увеличения сдвига02/04 до40%. Проверены globals.css, evening-plan-route.tsx, AGENTS.md, карта референсов, дизайн-система, отчёты маршрута и параллельной одобренной типографики Spectral/Golos/IBM Plex Mono. Сохраняются страница, четыре этапа, тексты, медиа, две desktop-колонки и отдельный поток<=900px.

План: увеличить первую строку desktop-сетки процесса на55px через calc(max(прежняя минимальная высота, измеренная сцена01) +55px). Это опускает03 целиком вместе с видео. Существующий расчёт04 сохраняет его прежнюю абсолютную позицию, когда действует проход под тарелкой; при необходимости04 следует ниже для сохранения порядка. Коэффициенты40%, проходы и форма пунктира остаются. До900px display:block игнорирует grid-template-rows, поэтому mobile не меняется.

Необоснованных шаблонных элементов нет. Все компоненты сохраняются; новый компонент, библиотека, оформление и текст не требуются. Решение опирается на текущее одобренное расположение и прямое уточнение. Проверить семь ширин, фактический сдвиг03, порядок, маршрут, подписи и переполнение. Публикация согласована с уже работающей шрифтовой задачей, с сохранением её файлов и предыдущих изменений.

## Результат проверки и отчёт

На1440/1280/1024px этап03 находится на654/655/634px ниже начала01. Во всех трёх случаях к прежней первой строке прибавлены ровно55px. На768/430/390/375px сохраняется вертикальный поток. Семь снимков визуально просмотрены: заголовки, видео, тарелка и подписи читаемы, пунктир не пересекает текст, горизонтального переполнения нет. Измерения: artifacts/process-step03-2026-09-13/stagger-metrics.json.

1. Созданы этот отчёт, локальные process-PNG семи ширин и JSON измерений; work/check-process-step03.mjs — временная локальная проверка.
2. Изменена одна декларация app/globals.css; обновлены AGENTS.md и три дизайн-документа. Параллельные шрифтовые изменения не входят в отдельный коммит03 и остаются в рабочей папке для общей публикации.
3. Необоснованных шаблонных элементов не найдено.
4. Изменено только положение03 через высоту первой строки; компоненты и маршрут сохранены.
5. Источники — прямое уточнение пользователя, текущая лесенка40% и утверждённые шрифтовые роли.
6. Решений без референса нет.
7. Новых UI-библиотек и компонентов нет.
8. Обязательные семь ширин визуально и геометрически проверены; антишаблонный аудит пройден. Общая сборка, действующие состояния и публикация проверяются вместе с финальной шрифтовой версией; результаты — в TYPOGRAPHY_ROLES_2026-09-13.md. Форм и пустых состояний данных здесь нет.
9. Ограничение — Chrome, без физического Safari/iOS. Отдельная публикация поверх параллельного релиза не запускается; согласованная общая версия сохраняет обе одобренные правки.
