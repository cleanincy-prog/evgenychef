# Новое фото «МастерШеф» — 2026-09-11

## Аудит и план до изменения интерфейса

Основание: прямой запрос пользователя заменить старое фото на присланное Photo 1.jpg (1280 × 1160). Проверены app/page.tsx, app/layout.tsx, app/globals.css, активные записи карты источников, дизайн-системы и плана. Старое фото используется в award-proof и одной из 63 ячеек коллажа. Сохраняются текущая композиция «План вечера», Caveat, бумажная палитра, подпись победителя, существующая адаптивная структура и все прочие компоненты.

Шаблонных элементов в изменяемой области не обнаружено. Переработка компонентов и новые компоненты не нужны. Старый горизонтальный кадр выводился через cover: новый почти квадратный снимок в той же маске обрезал бы лицо и конверт. План: сохранить оригинальный JPEG отдельным файлом, заменить оба вхождения, задать точные intrinsic размеры; в основном блоке оставить прежнюю долю ширины 23% / 36% и использовать естественную высоту с contain. В декоративной ячейке сохраняется cover по действующим правилам коллажа; проверить лицо. Старый файл остаётся для истории и отката. Никакой генерации, ретуши, смены цветов или библиотек.

Референсы: присланная фотография и одобренный текущий блок документального подтверждения. Новых решений без источника нет. Размеры снимка определяются реальным исходником. Проверить семь ширин 1440/1280/1024/768/430/390/375 px, отсутствие обрезки/переполнения, читаемость подписи и загрузку изображения. Публикация в существующие GitHub main и evgenychef.com разрешена действующей политикой AGENTS.md. Предыдущая параллельная правка Hero уже закоммичена отдельно (90676fe).

## Проверка и итоговый отчёт

- Созданы: `public/media/masterchef/evgen-masterchef-user-2026-09-11.jpg` (оригинал пользователя, 124413 байт) и этот отчёт.
- Изменены: `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `docs/DESIGN_REFERENCE_MAP.md`, `docs/DESIGN_SYSTEM.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/RELEASE_2026-09-11.md`.
- Шаблонных элементов не найдено; исправлено только кадрирование сменившего пропорции документального снимка, заменён источник в двух местах. Все существующие компоненты сохранены.
- Источники: присланный оригинал и одобренный интерфейс. Решений без референса нет; UI-библиотеки не добавлены.
- Встроенный браузер: 1440/1280/1024/768/430/390/375 px — фотография загружена, полный кадр сохранён, соотношение сторон 1.1035 соответствует 1280:1160, подпись не пересекает изображение, горизонтального переполнения нет. Все 63 фото коллажа загружены. На 375 px подпись занимает три строки, на 390/430 — две; читаемость сохраняется.
- HTTP JPEG возвращает 200 и побайтово совпадает с пользовательским файлом. Sites/Vinext build и пять существующих HTTP-тестов PASS; проверка diff PASS. В тесте обновлён ожидаемый путь старого снимка, новые тесты для простой замены не добавлялись.
- Изменяемое фото не интерактивно, не содержит формы, hover/focus/loading/empty/error элементов; соответствующие состояния существующих ссылок и видео не менялись. Публичные metadata/canonical и локальный noindex проверены существующими тестами.
- Финальный антишаблонный аудит: новых карточек, эффектов, декора, универсальных секций или библиотечного оформления нет. Цвета фото и страницы, шрифт Caveat, основной портрет, маршруты и всё содержимое сохранены. AGENTS.md остаётся без изменений.
- Ограничения проверки: физические телефоны отдельно не использовались. В Chrome при локальном preview наблюдалось существовавшее ранее зависание загрузки большинства изображений; оно не воспроизводится во встроенном браузере, JPEG исправен. Полный снимок доступен в основном блоке; декоративная ячейка коллажа сохраняет прежний cover. Открытых блокеров замены нет.

В выпуск включён уже отправленный отдельный коммит 90676fe5847114814597fadd37d522d8b7a1f8ac с адаптивной высотой Hero. Владелец параллельной задачи передал единую публикацию этой задаче. История, исходный старый JPEG и предыдущие Sites-версии сохраняются для отката. Итоговый SHA и результат размещения фиксируются в Git/Sites.
